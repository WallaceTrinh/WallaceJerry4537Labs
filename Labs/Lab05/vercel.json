{
  "version": 2,
  "builds": [
    {
      "src": "./server2/**/*.js",
      "use": "@vercel/node"
    }
  ],
  "routes": [
    {
      "src": "/api/(.*)",
      "dest": "/server2/$1.js"
    },
    {
      "src": "/(.*)",
      "dest": "/server1/$1"
    }
  ]
}
